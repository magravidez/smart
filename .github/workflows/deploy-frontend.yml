name: Deploy Frontend to GH Pages

on:
  push:
    branches: [master]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install deps
        run: npm ci --prefix frontend

      - name: Build
        run: npm run build --prefix frontend
        env:
          VITE_BASE: "/smart/"
          VITE_AIO_USERNAME: ${{ vars.VITE_AIO_USERNAME || secrets.VITE_AIO_USERNAME }}
          VITE_AIO_KEY: ${{ vars.VITE_AIO_KEY || secrets.VITE_AIO_KEY }}
          VITE_AIO_FEED: ${{ vars.VITE_AIO_FEED || secrets.VITE_AIO_FEED }}
          VITE_SUPABASE_URL: ${{ vars.VITE_SUPABASE_URL || secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ vars.VITE_SUPABASE_ANON_KEY || secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_SUPABASE_TABLE: ${{ vars.VITE_SUPABASE_TABLE || secrets.VITE_SUPABASE_TABLE }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
